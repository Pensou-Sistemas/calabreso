import 'package:flutter/material.dart';

class DSWAlert extends StatelessWidget {
  const DSWAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
