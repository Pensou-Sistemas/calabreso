import 'package:flutter/widgets.dart';

class DSWChip extends StatelessWidget {
  const DSWChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
